{
  "name": "specflow-code",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "bin": {
    "specflow": "./packages/cli/src/specflow.ts"
  },
  "scripts": {
    "dev": "bun run specflow",
    "specflow": "bun packages/cli/src/specflow.ts",
    "build": "bun run --cwd packages/ui build",
    "typecheck": "bun run --cwd packages/shared typecheck && bun run --cwd packages/agent-proxy typecheck && bun run --cwd packages/workflow typecheck && bun run --cwd packages/bridge typecheck && bun run --cwd packages/server typecheck && bun run --cwd packages/ui typecheck && bun run --cwd packages/cli typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "@vitejs/plugin-react": "^5.1.0",
    "typescript": "^5.9.3",
    "vite": "^7.2.2"
  }
}
